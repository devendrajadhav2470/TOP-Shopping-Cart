import React from "react";
import ReactDOM from "react-dom/client";
import RouteSwitch from "./RouteSwitch";
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <RouteSwitch />
  </React.StrictMode>
);

// okay so what is the thing, the thing is that i must move forward,
// i must build this app yep i must do it, this is the challenge
// okay so where do i even start with this project, it will take some time would it not
// yep it would take some time, what i must do is that
// i must do this, there is no doubt in that
// okay so i would have to what, i would have to create components laright
// yep i will have to create components, i have to create all that functionality
// so how do i do it,
